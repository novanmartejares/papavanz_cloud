# Vite frontend env. Vite proxies /api and /auth to the API in dev (see vite.config.ts).
# In production, the static build is typically served by the same origin as the API
# (e.g. behind Cloudflare Tunnel), so no separate URL is needed.
